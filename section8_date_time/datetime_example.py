import datetime as dt
from datetime import datetime

# today = datetime.today()
# print(today)
now = datetime.now() # now is preferred over today
utc_now = datetime.utcnow()

print(now)
print(utc_now)
